<h1>user一覧</h1>
<div>
  <% if @users.present? %>
    <% @users.each do |user| %>
      <p>
        <%= user.name %>
        <%= link_to('詳細', user_path(user)) %>
      </p>
    <% end %>
  <% end %>
</div>
<%= link_to('追加', new_user_path) %>
